# LEX-002 — 扩词到 B1+（目标 ~12000 词，license-clean 词表）

**类型**：后端 / 数据扩充（大批量）
**实现**：Codex1
**测试**：Codex2 + PM 抽检
**前置依赖**：LEX-001（词条管线已就绪）、LEX-CLEANUP-001（已 passing，construction 已落地）
**预估**：分阶段，2-3 天（含 PM 抽检循环）
**优先级**：中高（当前在线 733 词，A1-A2 够用但内容深度受限）

---

## 背景与目标

当前 DB：733 条 word-kind + 301 条多 token phrase-kind + 10 条 construction。词汇量停在 A1-A2，B1+ 内容查词时大量 miss → 回落到 DashScope 实时生成（慢、不可控、不入库）。

目标：把 lexicon 扩到 **~12000 词**（覆盖到 B1-C1 高频区），让绝大多数学习材料的查词命中本地库。

> ⚠️ **License 硬约束**：lexicon 后续要做成对外**计费制 API**，所有词表来源必须**允许商业再分发**。优先 CC-BY / MIT / Apache 等宽松许可；**禁止**直接使用受版权保护的成品词表（RAE/CREA、Instituto Cervantes PCIC、Routledge Frequency Dictionary 等）。词频列表只取「词形 + 频次」这类事实数据，不照搬他人的释义/分级文案。

---

## 词表来源（已核实许可证）

| 来源 | 许可证 | 用途 | 核实方式 |
|---|---|---|---|
| **hermitdave/FrequencyWords**（OpenSubtitles 2018 西语词频表）| **MIT** ✅ | **主来源**：取 top ~15k 词形按频次排序 | 已用 GitHub API 核实 `license.spdx_id = MIT` |
| wordfreq（Python 包）| 代码 Apache-2.0，**数据 NOASSERTION/混合** ⚠️ | 仅做**频次交叉验证**，**不再分发其数据** | PyPI metadata 核实 |
| KELLY project（EU CEFR 分级西语词表）| **未核实**（web 搜索后端当前不可用）| *可选* 分级参考，**许可证澄清前不接入** | 待 PM 手动核实 |

**决策**：本 ticket 先按 **FrequencyWords（MIT）单来源**走通。CEFR 分级不依赖外部分级表，改用 **DeepSeek 逐词判级**（见管线 step 4）。KELLY 若日后核实为干净许可，可作为分级双保险二次校准，不阻塞本 ticket。

---

## 管线（沿用 LEX-001 约定：默认 dry-run，`--write` 才落库）

新脚本目录沿用 `scripts/lexicon/`，CSV 中间产物放 `data/`，进度/跳过记录用 JSON（同 seed 脚本约定）。

1. **下载 + 解析词频表** `scripts/lexicon/download-frequency-words.mjs`
   - 拉 FrequencyWords 的 `es` full 列表 → `data/freq-es.txt`（词形 + 频次）
   - 记录来源 URL + commit hash + MIT 许可证声明到 `data/freq-es.LICENSE`（合规留痕，将来 API 要能说清来源）

2. **清洗 + 候选生成** `scripts/lexicon/build-wordlist-candidates.mjs --dry-run/--write`
   - 过滤：纯数字、单字母（保留 y/e/o/u）、专有名词大写残留、明显外来词/拼写噪声
   - lemma 归并（动词变位 → 原型；名词复数 → 单数）——复用现有 `pos-normalize.mjs` / 已有 lemmatize 逻辑，缺的补
   - **去重**：剔除 DB 已有的 733 word + 301 phrase（按 lemma）
   - 取频次 top ~15k 输出 `data/wordlist-b1-candidates.csv`（lemma, freq_rank, raw_freq）

3. **PM 抽检候选** — PM 看 CSV 头部/随机样本，确认噪声率可接受再放行下一步（避免给一堆垃圾词喂 LLM 烧钱）

4. **DeepSeek 逐词补全 + 判级** `scripts/lexicon/seed-b1-words.mjs --dry-run/--write`
   - 复用 `generate-phrase-candidates.mjs` 的 DeepSeek 批处理 + JSON 解析 + 错误隔离/跳过模式（**保留 skip 日志** `data/lexicon-b1-skipped.json`，吸取 LEX-004 教训）
   - 每词让 DeepSeek 产出：`translationZh` / `partOfSpeech` / `cefr`(A1-C2) / `exampleEs` + `exampleZh` / `morphology`
   - 取判级为 **B1-C1** 的词入库（A1-A2 已有；C2 暂不要，价值低）目标净增 ~12000
   - `--resume` 支持断点续跑（同 seed-a1-a2-words 约定），`--concurrency` 可调

5. **入库后复核**
   - 计数：word-kind 总数应 ≈ 733 + 净增量
   - 抽查例句质量 + 判级合理性（PM 随机抽 30-50 条）
   - 跑 phrase 高亮/lookup 回归，确认没破坏现有体验

---

## 成本与体量（已估算）

- **存储**：每条 word entry ~1-2KB（含 morphology JSON + 例句），12000 词 ≈ 15-25MB，对 Neon 完全无压力
- **LLM 成本**：DeepSeek V3 批处理 12000 词，单价极低，预估个位数美元级
- 真正的成本是 **PM 抽检/质量把关的人力**，所以管线必须在 LLM 烧钱前先过 step 3 的候选抽检闸门

---

## 不在本 ticket 范围

- ❌ B1+ 短语/习语扩充（那是 LEX-004 backlog）
- ❌ KELLY 或其他外部分级表接入（许可证未澄清前不碰）
- ❌ 前端展示变化（纯数据扩充，查词 UI 不变）
- ❌ C2 / 超低频长尾词

---

## 验收标准

- [ ] PM 确认词表来源 + 许可证留痕（`data/freq-es.LICENSE` 含来源 URL/commit/MIT 声明）
- [ ] step 2 候选 CSV 生成，PM 抽检噪声率可接受
- [ ] `seed-b1-words.mjs` 默认 dry-run，`--write` 落库，`--resume` 可续跑，skip 有日志
- [ ] 入库后 word-kind 净增接近目标，judged B1-C1，抽检 30-50 条质量过关
- [ ] 301 条 multi-token phrase + 10 条 construction **不受影响**（计数复核）
- [ ] `npm test` 通过
- [ ] PM 实测若干 B1 词（之前会 miss 回落 DashScope 的）现在本地命中

---

## 执行流程

```
Claude1（本 ticket）
  → Codex1 step1-2（下载/清洗/候选 CSV）
    → PM 抽检候选闸门（step 3）
      → Codex1 step4（DeepSeek 判级补全 + --write 落库）
        → Codex2 回归测试
          → PM 抽检质量 + 实测命中 → passing
```
