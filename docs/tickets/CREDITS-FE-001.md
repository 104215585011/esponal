# CREDITS-FE-001 — 积分前端(web + 移动端)

**优先级**：P1(C 变现的用户可见层)
**区域**：余额展示(顶栏/导航/侧边栏)+ 会员定价页 + 账户/积分页 + 积分不足提示;**web 桌面端 + 移动端都要**
**设计**：会员定价页走 design 模型(同移动那套流程);其余照设计语言直接实现
**实现**：Codex1(前 + 少量后端:余额 API)
**测试**：Codex2 + 用户真机 + PM 验收
**依赖**：积分引擎 Phase 1 ✅(`src/lib/credits/`);积分不足提示依赖 Phase 2(扣费返回 ok:false);购买依赖支付(独立 spec,本票按钮占位)
**预估**：2 天(含定价页设计)

---

## 背景
积分引擎 Phase 1 已实现(数据+扣费+刷新+赠送),但**完全没有用户可见的前端**。积分是新功能,**web 桌面端也需要加**(之前没有任何积分 UI)。本票补齐积分的全部前端表面,web + 移动双端。

## 设计基准
- 遵守 `docs/tickets/MOBILE-design-language.md`(干净现代:白底/Plus Jakarta+Noto Sans SC/翡翠点缀/轻卡片)。
- **会员定价页是旗舰页**:参考用户认可的 **DejaVocab 定价页**(月付/年付/共建者三 tab,卡片式套餐),先出**设计模型**(HTML)给 PM/用户批准,再实现(同首页/课程那套"先看模型"流程)。
- 桌面端不回退;移动端复用外壳(顶栏/底部 tab)。

## 要做的表面(web + 移动都要)

### A. 配额余额展示 + 入口
- **移动端**:顶栏/侧边栏(MOBILE-009 头像侧边栏已有"Esponal 积分"项)显示**当前配额余额**(如 `⚡ 320`)+ 点进账户/会员页。
- **web 桌面端**:站点顶栏导航(SiteNav/SiteHeader)加积分余额 + 会员入口(之前桌面无任何积分 UI,需新增)。
- 余额来源:新增 **`GET /api/credits`**(返回 `{ plan, balanceDisplay, balanceMinor }`,登录用户;调 `getBalanceMinor`)。

### B. 会员/订阅(定价)页 `/membership`(旗舰,出模型)
- 三 tab:**月付 / 年付 / 共建者(终身)**。
- 套餐(数据见下"等级表"):免费 / 进阶 ¥38月·¥365年 / 高阶 ¥48月·¥458年 / 终身进阶 ¥1498 / 终身高阶 ¥1998;标注每月配额、试用、推荐、终身限量。
- **购买按钮 = 占位/禁用态**(标"即将开放"或类似),真正支付走独立 spec。当前等级高亮"当前方案"。
- web + 移动响应式。

### C. 账户/积分页(或并入个人中心)
- 当前等级、配额余额、**用量流水**(CreditTransaction:时间/动作/±配额/余额)。web + 移动。

### D. 积分不足内联提示(依赖 Phase 2)
- 动作扣费返回 ok:false 时,**非模态**内联提示"配额不足 · 升级解锁" + 跳 `/membership`。不弹窗。
- 本票先做 UI 组件 + 接已就绪的扣费返回;Phase 2 接好消费点后联通。

## 等级表(定价页数据,来自 spec)
| 等级 | 月付 | 年付(省16%) | 月配额 | 试用 | 备注 |
|---|---|---|---|---|---|
| 免费 | ¥0 | ¥0 | 注册一次性 50 | — | 当前默认 |
| 进阶 ⭐ | ¥38 | ¥365 | 500/月 | 3天 | 推荐 |
| 高阶 | ¥48 | ¥458 | 1000/月 | 3天 | |
| 终身进阶 | ¥1498 一次性 | — | 500/月累加 | — | 限量500 |
| 终身高阶 ⭐ | ¥1998 一次性 | — | 1000/月累加 | — | 限量500 |

## 验收标准
- [ ] web + 移动 都能看到当前配额余额 + 会员入口。
- [ ] `GET /api/credits` 返回登录用户的 plan + 余额(展示值)。
- [ ] `/membership` 定价页(三 tab + 套餐)符合批准模型,web/移动响应式;购买按钮占位、当前方案高亮。
- [ ] 账户页展示等级 + 余额 + 用量流水。
- [ ] 积分不足内联提示组件就绪(非模态)。
- [ ] 强调色翡翠绿、禁 sky;触摸≥44px;桌面不回退;UTF-8。
- [ ] 真机/桌面实际打开各页不崩、不乱码;`npm test` 全绿 + build + lint:encoding。

## 不在范围
- ❌ 支付/订阅扣款/真正购买(独立 spec)。
- ❌ Phase 2 的消费点扣费接入与门槛(单独;本票只做不足提示的 UI 接口)。

## 流程
Claude1(本票)→ **会员定价页出 design 模型** → PM/用户批准 → Codex1 实现(+ 余额 API)→ Codex2 真机 → 用户真机 → Claude1 验收。
> 血泪三戒:复用外壳/设计语言、桌面不回退、真机验、UTF-8、勿带临时文件/worktree 污染。
