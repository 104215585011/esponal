# EXT-008 — Chrome 扩展字幕采集（EXT-006 复活精简版）

**优先级**：P1（直接决定 `/watch` 字幕质量天花板）
**区域**：extension / web
**依赖**：WEB-003 ✅（/watch 已存在）、WEB-004 ✅（subtitle 路由已存在）、OPS-002 ✅（rate limit 基础设施）

---

## 背景

我们 `/watch` 页字幕来源目前是 Apify YouTube 抓取器 + 本地 Whisper 兜底。Apify 受 YouTube 反爬（PO Token 验证）影响，对大量教学类视频要么拿不到字幕、要么拿到降级版本。Spike 验证（2026-05-20，HAR 文件分析 + youtubei.js PO Token 测试）确认：

- **西语助手（Eudic Chrome 扩展）**之所以字幕质量好，**就是**因为它在用户已登录的浏览器里跑 content script，直接 fetch `youtube.com/api/timedtext`，命中带 PO Token 的官方 JSON3 接口
- **服务器端纯 PO Token 方案**（路径 2B）经 spike 验证不可行——多数视频返回 `LOGIN_REQUIRED: Sign in to confirm you're not a bot`
- 用户**主动浏览 YouTube** + 我们扩展被动采集，是**唯一**能稳定拿到等同西语助手质量字幕的路径

我们曾在 EXT-006/EXT-007 实现过这套方案，2026-05-18 commit `81c8ad3` 整体删除，原因：
1. Chrome 138+ 阻断 `--load-extension` 让 Playwright 自动批量预采集不可行（**这条限制依然存在，不复活该路径**）
2. 当时给"想象中的测试者"用，认为安装门槛过高（**现在场景明确：用户就是自己，门槛不再是阻力**）

本票复活原 EXT-006 的"用户安装扩展 + 浏览 YouTube 时自动采集"路径，**不**复活 EXT-007 的 Playwright 批量预采集。

---

## 用户故事

1. 用户去 `chrome://extensions` 加载 Esponal 扩展（开发者模式手动加载，或未来从 Chrome Web Store 装）
2. 用户照常在 YouTube 看西语视频，扩展**全程透明**采集字幕回 Esponal DB
3. 用户切回 Esponal `/watch?v=同 ID` → 立即命中高质量字幕（YouTube 官方轨道，与西语助手等价）
4. 用户打开**没访问过 YouTube 的视频**时 → Esponal 静默走 Apify/Whisper 兜底；兜底也失败时，UI 提示用户"在 YouTube 看一遍即可自动归档"

---

## 三个决策（已与 PM 确认）

| 决策 | 选项 |
|------|------|
| Ingest 接口认证 | **保留 `X-Esponal-Ingest-Token`**，token 在扩展打包时注入；视作垃圾流量过滤而非安全密码；主要防护靠 IP rate limit |
| `/watch` 字幕命中失败时 | **三段式**：先查 Redis 库存（扩展灌入的）→ 没有就走 Apify → Apify 也空就显示"在 YouTube 看一遍"提示 |
| 扩展触发时机 | **任意** `youtube.com/watch*` 自动触发；只采集 `es-*` 轨道；无西语轨道时静默放弃 |

---

## 技术方案

### 1. 扩展侧：`extension/harvest.js`（content script）

**Bridge 模式**（与原 EXT-006 一致，已验证可行）：

```js
// 1. 注入一段 <script> 到页面，运行在页面 JS 上下文里
//    读取 window.ytInitialPlayerResponse.captions.playerCaptionsTracklistRenderer.captionTracks
// 2. 用 window.postMessage 把数据扔回 content script（隔离世界）
// 3. content script 收到后，对每个 es-* 轨道 fetch 它的 baseUrl + "&fmt=json3"
//    fetch 时 credentials: "include" → 浏览器自动带 YouTube cookie + PO Token
// 4. 解析 JSON3 → cue 数组 → POST 到 /api/subtitle/ingest
```

**采集触发**：每次落到 `https://www.youtube.com/watch*` 页面时跑一次。用 `window.__esponalHarvested = videoId` 防同一视频重复采集。

**最小阈值**：cue 数 < 5 不上报（极短或异常轨道丢弃）。

### 2. 扩展侧：`extension/parseJson3.js`

把 YouTube JSON3 `events[]` 结构（包含 `tStartMs`、`dDurationMs`、`segs[].utf8`、`tOffsetMs`）解析成我们标准 `{ start, dur, text }[]`。直接复用旧 EXT-006 实现（commit `2a281ce`）。

### 3. 扩展侧：`extension/manifest.json`

新增一条 content script 注册：

```json
{
  "matches": ["https://www.youtube.com/watch*"],
  "js": ["dist/harvest.js"],
  "run_at": "document_idle"
}
```

`host_permissions` 已含 `https://www.youtube.com/*`，无需变更。

### 4. 扩展侧：`extension/scripts/build.mjs`

esbuild 把 `harvest.js` + `parseJson3.js` 打成单个 `dist/harvest.js`。打包时通过 `--define` 注入：

```
ESPONAL_APP_ORIGIN  → 默认 https://esponal.example.com（占位，实际生产 URL）
EXT_INGEST_TOKEN    → 从环境变量 EXT_INGEST_TOKEN 注入
```

本地开发时 `ESPONAL_APP_ORIGIN=http://localhost:3000 EXT_INGEST_TOKEN=dev-token npm run build` 即可。

### 5. 扩展侧：`extension/popup.html` + `popup.js`

popup 新增**一块"最近采集"卡片**，位置在标题/说明文字下方、两个主按钮**上方**（动态状态在恒定动作之上）。

数据来源：`chrome.storage.local.get("lastSubtitleHarvest")`，扩展采集时写入：
```js
{
  videoTitle: document.title.replace(/ - YouTube$/, ""),  // 必存
  durationSec: cues[cues.length-1].start + cues[cues.length-1].dur,  // 用于显示时长
  harvestedAt: new Date().toISOString()
}
```

**绝不显示 video ID**（对学习者零意义且像 bug）。视觉规格用紧凑卡片：

```html
<div class="recent-harvest">
  <div class="recent-harvest-label">刚刚采集了一段字幕</div>
  <div class="recent-harvest-meta">{title 截断到 30 字} · 约 {N} 分钟字幕 · {相对时间}</div>
</div>
```

```css
.recent-harvest {
  margin-top: 14px;
  padding: 10px 12px;
  background: #F5F4F2;
  border-radius: 6px;
}
.recent-harvest-label { font-size: 12px; color: #52616f; }
.recent-harvest-meta  { font-size: 13px; color: #1f2933; margin-top: 4px; font-weight: 600; }
```

**时长换算**：`Math.round(durationSec / 60)` → "约 X 分钟字幕"；< 60 秒显示 "不到 1 分钟字幕"。

**相对时间**：用浏览器原生 `Intl.RelativeTimeFormat("zh-CN")`，零依赖；五档够用："刚刚 / N 分钟前 / N 小时前 / 昨天 / N 天前"。popup 打开时计算一次即可，无需 tick。

**不要显示**：video ID、`lang` 字段、cue 条数、总采集量统计（违反 Esponal 反游戏化原则）。

**无数据时**：显示 "尚未采集任何字幕，去 YouTube 看几个西语视频试试"。

其余 popup 内容（标题、两个主按钮）不动。

### 6. 服务器侧：`src/app/api/subtitle/ingest/route.ts`

直接复刻旧 EXT-006 版本（commit `2a281ce`），关键参数：

- `X-Esponal-Ingest-Token` header 校验
- IP 级 rate limit（用 `ingestLimiter`）
- payload 大小上限 500KB
- cue 数量 5 ~ 5000
- 单 cue text 长度 < 500、dur 0~600 秒
- `videoId` 字符集 `[\w-]+` 长度 ≤ 32
- `lang` 格式 `^[a-z]{2}(?:-[A-Za-z0-9]+)?$`
- 写 Redis key `subtitle:v4:${videoId}:${lang}:auto`，TTL 30 天
- 已有该 key 时**不覆盖**（防止低质量采集覆盖已有高质量）
- 返回 `{ success, cueCount, written: boolean }`

### 7. 服务器侧：`src/lib/ratelimit.ts`

复活 `ingestLimiter`：每 IP 60 秒 30 次（用户一次浏览只触发一次 ingest，30 次已经很宽松）。

### 8. 服务器侧：`/watch` 三段式 fallback（**新逻辑，不在旧 EXT-006 范围**）

修改 `src/app/api/subtitle/route.ts` 的字幕获取流程：

```
1. 先查 Redis subtitle:v4:${videoId}:${lang}:auto
   ↳ 命中 → 返回（这一步现状已有）
2. 未命中 → 调 Apify（现状已有）
3. Apify 返回 0 cue → 在响应 body 加 hint 字段：
   { cues: [], hint: { reason: "no_subtitle" } }
```

### 8a. 扩展安装检测

content script `harvest.js` 在 `youtube.com/watch*` 跑时，同时也要为我们自家站点服务——加一条 content script 注册到 `https://esponal.example.com/*`（生产 URL）和 `http://localhost:3000/*`（dev），跑一行：

```js
document.documentElement.setAttribute("data-esponal-ext", "1");
```

这是检测扩展是否安装的行业标准做法，比 postMessage ping 简单可靠。

### 8b. 客户端引导卡（区分"已装" vs "未装"）

`TranscriptPanel.tsx` 在 `cues.length === 0 && hint?.reason === "no_subtitle"` 时，渲染 EmptyState（**必须复用** `src/app/components/ui/EmptyState.tsx`，不要自己写）。

先用 `useEffect` 读 `document.documentElement.dataset.esponalExt === "1"` 判断扩展是否安装，分两种 UI：

**已装扩展**：
```tsx
<EmptyState
  kind="empty"
  title="这个视频暂时没有高质量字幕"
  description="去 YouTube 看一遍，扩展会自动采集回来"
  action={{
    label: "在 YouTube 打开",
    href: `https://www.youtube.com/watch?v=${videoId}`,
    external: true
  }}
/>
```

**未装扩展**：
```tsx
<EmptyState
  kind="empty"
  title="这个视频暂时没有高质量字幕"
  description="装上 Esponal 扩展后，在 YouTube 看一遍即可自动归档"
  action={{ label: "安装扩展", href: "/extension" }}
  secondaryAction={{
    label: "先去 YouTube 看",
    href: `https://www.youtube.com/watch?v=${videoId}`,
    external: true
  }}
/>
```

### 8c. EmptyState 增强（`src/app/components/ui/EmptyState.tsx`）

现 `EmptyState` 的 `action.href` 用 `<a>` 但**没有** `target="_blank"`。**新增**：

- `action.external?: boolean` —— 为 true 时渲染 `target="_blank" rel="noopener noreferrer"`
- `secondaryAction?: { label, href, external? }` —— 次按钮，视觉重量低于主按钮（灰色描边而非 brand 填色）

改动控制在 EmptyState 组件内部 + 类型定义，不影响现有调用方。

### 8d. 采集成功反馈

**完全静默 + popup 状态 + 浏览器原生 badge**（不在页面上画任何动画——违反 Esponal 反游戏化原则）。

采集成功时在扩展 background script 发：
```js
chrome.action.setBadgeText({ text: "✓" });
chrome.action.setBadgeBackgroundColor({ color: "#4CAF50" });
setTimeout(() => chrome.action.setBadgeText({ text: "" }), 3000);
```

Badge 是浏览器扩展图标右下角的原生小标签，不污染 YouTube 页面 DOM。

### 9. 扩展打包与分发

- `npm run package`（in `extension/`）打 zip → `public/extension/esponal-extension.zip`
- 用户从 `/extension` 落地页下载 zip → Chrome `chrome://extensions` 开发者模式 → "加载已解压的扩展程序"
- 现有 `/extension` 落地页（EXT-005 ✅）的安装步骤说明保持，无需新页面

### 10. 环境变量

`.env.example` 新增：
```
EXT_INGEST_TOKEN=""
ESPONAL_APP_ORIGIN="http://localhost:3000"
```

生产 Vercel env 配置同名变量（PM 部署前手动加）。

---

## 验收标准

| # | 检查项 |
|---|--------|
| 1 | `extension/harvest.js` + `extension/parseJson3.js` 存在 |
| 2 | `extension/manifest.json` 注册两条 content script：`dist/harvest.js` 在 `youtube.com/watch*`，并新增一条在 Esponal 域名设置 `data-esponal-ext="1"` 的脚本 |
| 3 | `extension/scripts/build.mjs` 用 esbuild 打包，支持 `--define` 注入 `EXT_INGEST_TOKEN` 与 `ESPONAL_APP_ORIGIN` |
| 4 | `extension/popup.html` + `popup.js` 显示紧凑卡片："`{视频标题截断} · 约 N 分钟字幕 · {相对时间}`"；**不显示** video ID / lang / cue 条数；无数据时显示引导文案 |
| 5 | popup 用 `Intl.RelativeTimeFormat("zh-CN")` 计算相对时间 |
| 6 | 扩展采集成功时调 `chrome.action.setBadgeText({text:"✓"})` 持续 3 秒后清除；**不**在 YouTube 页面 DOM 上画任何反馈 |
| 7 | `src/app/api/subtitle/ingest/route.ts` 存在，含 token 校验 + rate limit + payload 校验 |
| 8 | Ingest 接口已有 key 时**不覆盖**返回 `{ written: false }` |
| 9 | `src/lib/ratelimit.ts` 含 `ingestLimiter` |
| 10 | `src/app/api/subtitle/route.ts` 在 Apify 返回 0 cue 时，response body 含 `hint.reason === "no_subtitle"` |
| 11 | `src/app/components/ui/EmptyState.tsx` 新增 `action.external?: boolean` 与 `secondaryAction?` 字段 |
| 12 | `src/app/watch/TranscriptPanel.tsx` 在空字幕状态用 `EmptyState`（复用，不自写），且 `useEffect` 读 `data-esponal-ext` 后**分两种文案 + 两种 action 组合**（已装/未装） |
| 13 | `.env.example` 含 `EXT_INGEST_TOKEN` 与 `ESPONAL_APP_ORIGIN` |
| 14 | `npm run package`（in `extension/`）生成包含 `dist/harvest.js` 的 zip |
| 15 | `npm test` 全部通过，`npm run build` 通过 |

---

## 不在本票范围

- **EXT-007 Playwright 批量预采集**——Chrome 138+ 限制依然存在，不复活
- 上传扩展到 Chrome Web Store 发布（用户手动加载就行）
- 多语言扩展支持（只处理 es-* 轨道）
- 服务器端 PO Token 生成（spike 已证明不可行）
- 字幕版本管理（多次采集合并 / 用户编辑订正等）
- 把 ingest 数据归到具体用户账户（当前是匿名采集到全局缓存，不绑用户）
- 浏览器扩展的更新提示机制
- 离线模式下的扩展行为（无网络时静默放弃）

---

## 文件触动清单

| 文件 | 变更 |
|------|------|
| `extension/harvest.js` | 新建（参考旧 commit `2a281ce`） |
| `extension/parseJson3.js` | 新建 |
| `extension/scripts/build.mjs` | 新建，esbuild 打包 + define 注入 |
| `extension/package.json` | `build` 脚本改用 `scripts/build.mjs` |
| `extension/scripts/package.mjs` | 更新：zip 含 `dist/harvest.js` |
| `extension/manifest.json` | 新增第二条 content_script |
| `extension/popup.html` | 加 "最近采集" 区块 |
| `extension/popup.js` | 读 `chrome.storage.local.lastSubtitleHarvest` 并渲染 |
| `src/app/api/subtitle/ingest/route.ts` | 新建 |
| `src/lib/ratelimit.ts` | 新增 `ingestLimiter` |
| `src/app/api/subtitle/route.ts` | Apify 0 cue 时返回 hint 字段（现有结构兼容） |
| `src/app/components/ui/EmptyState.tsx` | 新增 `action.external?: boolean` 与 `secondaryAction?` 字段 |
| `src/app/watch/TranscriptPanel.tsx` | 空字幕状态用 EmptyState 渲染，useEffect 检测 `data-esponal-ext` 分已装/未装两种文案 |
| `.env.example` | 新增 `EXT_INGEST_TOKEN`、`ESPONAL_APP_ORIGIN` |
| `tests/ext008.test.mjs` | 新建（源码契约 + ingest 路由契约） |
| `feature_list.json` | 新增 EXT-008 |
| `session-handoff.md` | Dev Report |

---

## 测试要点

`tests/ext008.test.mjs`（沿用项目惯例的源码契约风格）：

1. `extension/harvest.js` 含 `ytInitialPlayerResponse`、`postMessage` bridge、`fmt=json3`、`credentials: "include"`、`/api/subtitle/ingest` POST 调用
2. `extension/parseJson3.js` 导出 `parseJson3ToCues` 且能解析含 `events[].segs[].utf8` 的 fixture
3. `extension/manifest.json` 含两条 content_scripts，第二条注册到 Esponal 域名设置 `data-esponal-ext`
4. `extension/scripts/build.mjs` 含 `esbuild` 调用 + `--define` 注入两个变量
5. `extension/popup.html` 含 `.recent-harvest` 区块；`popup.js` 含 `Intl.RelativeTimeFormat` 与时长换算 `Math.round(durationSec / 60)`；**不出现** `videoId` 字符串
6. 扩展 background script 含 `chrome.action.setBadgeText({text:"✓"})` 调用
7. `src/app/api/subtitle/ingest/route.ts` 含 token 校验、rate limit、payload 校验、`written: false` 不覆盖逻辑
8. `src/lib/ratelimit.ts` 导出 `ingestLimiter`
9. `src/app/api/subtitle/route.ts` 在空 cue 时返回含 `hint.reason === "no_subtitle"` 的响应
10. `src/app/components/ui/EmptyState.tsx` 类型定义含 `external?: boolean` 与 `secondaryAction?:` 字段；模板渲染时 `external=true` 加 `target="_blank" rel="noopener noreferrer"`
11. `src/app/watch/TranscriptPanel.tsx` 含 `useEffect` 读 `data-esponal-ext`、含两种文案分支（"扩展会自动采集回来" / "装上 Esponal 扩展后"）
12. `.env.example` 含两个新变量

---

## UI 评审重点（PM 在派 Codex1 前先过 Claude2）

- **popup 的 "最近采集" 区块视觉** —— 沿用 EXT-005 落地页风格还是新设计？字号、布局、空状态文案
- **`/watch` 字幕空状态引导卡** —— 与 WEB-011 共享 EmptyState 组件的兼容性？"在 YouTube 打开" 按钮的视觉重量
- **是否需要在扩展 popup 显示总采集数 / 总采集视频数** 作为用户成就感反馈
- 是否需要在采集成功时给页面一个**轻微的可见反馈**（比如右上角飘一个小绿点 0.5 秒）让用户知道扩展在工作；或保持完全静默（西语助手是有反馈的）
